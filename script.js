let a = '12.4';
let b = 36;
let c = '90';

function myFn (a, b, c,) {
   if (a == Number,
      b == Number,
      c == Number);
       else  ( 
         a == String,
         b == String,
         c == String)
        {
         a = +a;
         b = +b;
         c = +c;
         console.log( typeof a)
        }
   
   return a * b * c;
}
console.log(myFn(a, b, c))